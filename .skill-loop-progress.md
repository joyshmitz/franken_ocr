# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: franken_ocr current performance-critical paths
# Total Campaign Steps: 15
# Performance Experiments: 12
# Started: 2026-07-10T02:08:00Z

## Status: 12 experiments plus 3 conformance/audit steps complete; strict current-tree revalidation in progress

## Iteration Accounting

- Steps 1-12 are the campaign's genuine performance experiments. Step 13 is a
  precision/conformance rejection, step 14 is a final audit, and step 15 is a
  correctness acceptance replay. The latter three are not additional
  `extreme-software-optimization` iterations.
- The 14 missions below are a roadmap, not a serial mapping to the 15 campaign
  steps. Deferred or blocked missions remain explicitly non-experimental.
- This file does not claim 15 independent invocations of
  `repeatedly-apply-skill`: the historical work was bundled across commits and
  did not create one fresh subagent and one commit per step. The defensible
  record is 12 distinct exploratory performance experiments with three
  subsequent conformance/audit steps.

## Evidence Integrity Reconciliation (2026-07-11)

- Five retained focr-only results are classified as
  `PROVISIONAL_LOCAL_WIN`, not `WIN`, until `bd-2mo.30.15` clears the strict
  current-tree/reference protocol.
- Passes 2-11 now have closed machine-validated experiment cards. Nine legacy
  A/B manifests were normalized from repository-relative to evidence-local
  targets; the gauntlet verifier resolves all 14 registered hypotheses and the
  11-round convergence tail remains clean at 0/0.
- The TrOMR opaque-alpha discrepancy is uniquely identified as `DISC-007`;
  `DISC-004` remains the multi-page precision-trajectory record. The validator
  now rejects duplicate discrepancy IDs and unsupported outcome labels.
- A fresh bounded CASS query again timed out after 20 seconds and is recorded as
  negative preflight evidence with a committed-evidence-only fallback.
- Strict performance timing remains blocked by the quiet-host gate. The latest
  one-minute load sample was 4.25, above the required threshold of 2.0; no
  forced or publishable timing row was produced.

## Governing Issue

- Campaign: bd-2mo.30 (kept open for the release-grade follow-up gates)
- Completed hardening: bd-2mo.30.1, bd-2mo.30.2, bd-2mo.30.3,
  bd-2mo.30.4, bd-2mo.30.5, bd-2mo.30.6, bd-2mo.30.10,
  bd-2mo.30.13
- Open release/integration gates: bd-2mo.30.14, bd-2mo.30.15
- Local acceptance evidence now satisfies the page/corpus predicate owned by
  bd-2mo.30.12; tracker closeout, publication, regenerated certification, and
  distribution receipts remain pending.
- Provisional performance beads blocked on strict revalidation: bd-2mo.30.9,
  bd-2mo.30.11
- Rule: profile first, prove behavior, one lever per pass, keep only measured wins.

## Strict Current-Tree Revalidation Protocol (bd-2mo.30.15)

This contract was fixed before current-tree timing results were visible:

- Build: clean committed `main`, trusted `release-perf` source/build receipt,
  system allocator, Apple M4, eight pinned workers, conservative recipe artifact
  SHA-256 `573340710167697891bf52dfa4cbb5d0a02a68f3011c01f8ef83fd34622fb592`.
- Baselines: one discarded warmup plus 20 measured runs for full-output sparse
  `page_0009` and dense `page_0014`; report best/p50/p95/p99, CV, stage times,
  throughput, and peak RSS. The 10-page and 20-page `ocr-batch` baselines use
  the sorted real corpus, `FOCR_MAX_NEW_TOKENS=32`, one warmup, and 20 measured
  runs so vision/cache scaling is bounded while decode remains represented.
- A/B schedule: one warmup and five measured runs per arm in the harness's
  balanced `A B B A / B A A B / A B` schedule (the four-run pattern, truncated
  after five runs per arm). Report best/p50/p95/p99 and CV for every stage and
  RSS; p95/p99 from five samples are explicitly conservative.
- Instrumentation: every retained-win capture sets `FOCR_PROFILE_DECODE=1`.
  The harness parses both conservative `decode phases` and full-int8
  `decode_i8 phases` lines into `decode_lm_head`, `decode_attn`,
  `decode_experts`, and `decode_route` raw and aggregate stages.
- Correctness: every output must be byte-identical across arms. Sparse/dense
  defaults must match their pinned goldens; capped batches must be semantically
  identical after timing fields are removed. Any difference rejects the lever.
- Noise and keep gate: both arms require CV <= 5%. A keep needs at least 2%
  median improvement in its predeclared focused stage, no greater than 1%
  median regression in the broad end-to-end gate, and no pass-over-pass ratchet
  violation. Otherwise default the lever off or source-revert it and record a
  concrete retry predicate in `docs/NEGATIVE_EVIDENCE.md`.
- Lever/workload map: parallel R-SWA heads on dense and capped 20-page decode;
  mmap on sparse/dense startup; softmax ownership on sparse/dense plus capped
  10/20-page vision; Unlimited vision statics on capped 10/20-page batches; Apple
  autovec on dense plus capped 10/20-page expert/decode stages.
- Host gate: do not cite runs captured while another build, profiler, or heavy
  application is active. A noisy capture remains diagnostic only and is rerun.

## Missions

1. Current-HEAD sparse/dense/10/20 profiling handoff: produce the fingerprint, 20-run baselines, CPU/allocation/I/O/off-CPU evidence, ranked hotspots, and hypothesis ledger. The checked-in corpus contains 20 real pages; `._*` entries are metadata, not pages.
2. SAM MLP/global-attention top frame: attack only a measured SAM frame, excluding rejected row tiling and polynomial-exp retries.
3. MoE expert cache-contiguous packing and prefetch: reduce expert-stage weight stalls without changing expert selection or arithmetic.
4. Fused gate/up activation quantization and SwiGLU: share exact input quantization and remove materialization while preserving accumulation and activation semantics.
5. Bit-exact R-SWA decode attention: improve the measured attention path without the previously rejected reordered GEMM or int8-KV numerics.
6. Lm-head scheduling or certified search: reduce exact greedy argmax work while preserving bans and lowest-token-id tie breaking.
7. Grouped expert GEMMs for prefill: improve measured grouped-MoE execution without changing scatter/combine order.
8. Dequant-GEMM-bias-activation fusion: fuse only where the current profile attributes enough time and the integer/f32 proof is explicit.
9. RMSNorm-RoPE-residual fusion: remove passes while preserving f32 order within the conformance tolerance.
10. Allocation-trace-driven buffer reuse and alignment: change ownership/layout only after allocation evidence identifies a live cost.
11. AF-5 thread/USL sizing: fit per-op scaling and retain the deterministic physical-core fallback.
12. Continuous-batch and L3-resident throughput: test 10/40/500-page regimes with bounded memory and forced degradation tiers.
13. AVX-VNNI and AVX-512 validation: repeat the current-shape evidence loop on a matching x86 host; do not extrapolate from M4.
14. PGO/BOLT and final reprofile: rerun head-to-head, parity, ledgers, convergence, and release certification on the final tree.

## Eligibility Reconciliation

- The initial profile was an attribution profile of the experimental full-int8
  configuration. Its timing lane was ineligible, its strict 20-run timing gate
  was not completed, and the 10/20-page and allocation lanes remained open. It
  supported hotspot discovery and correctness hardening; it did not authorize a
  release-performance promotion.
- Passes 1-13 are exploratory/local evidence. Their retained changes passed the
  stated page-byte and focused correctness checks, but their performance claims
  remain provisional until `bd-2mo.30.15` reruns sparse, dense, 10-page, and
  20-page measurements on a trusted current-tree `release-perf` binary with the
  current keep gate, sample count, and coefficient-of-variation reporting.
- Pass 13 is diagnostic negative evidence under the legacy CER receipt. It is
  sufficient to reject the experimental full-int8 configuration, but it is not
  eligible for strict certification under the new physical-replay contract.
- A clean current-tree release build and profile were not manufactured from the
  shared dirty checkout. The trusted-build receipt now refuses that state, and
  `bd-2mo.30.15` owns the clean-tree revalidation instead of relabeling older
  binaries or measurements.
- This campaign may finish as an exploratory optimization and hardening audit.
  It cannot claim release certification or final-tree performance until the
  named follow-up gates close.
- Pass 15 adds a hash-bound local correctness receipt, not a clean-tree
  performance or distribution certificate. Its source identity is the baseline
  head plus the recorded uncommitted final-tree MoE/loader changes.

## Mission Dispositions

- Mission 3, expert packing/prefetch: deferred to `bd-2mo.13`; alien candidate
  A3 remains proposal-only pending a measured expert-bandwidth profile.
- Mission 4, fused gate/up quantization and SwiGLU: deferred because the initial
  profile did not authorize a source optimization and no strict current-tree
  attribution proved the materialization hot enough.
- Mission 7, grouped expert prefill: deferred; the measured exploratory profile
  did not identify it as the next eligible hotspot.
- Mission 8, dequant/GEMM/bias/activation fusion: deferred until a strict profile
  attributes enough time and the integer/f32 ordering proof is specified.
- Mission 9, RMSNorm/RoPE/residual fusion: deferred for the same attribution and
  arithmetic-order proof requirements.
- Mission 10, allocation-driven reuse/alignment: blocked on a valid bounded
  allocation trace; the attempted capture did not produce eligible evidence.
- Mission 12, continuous batching/L3 throughput: deferred to the existing
  batching/cache lanes and blocked here by the missing strict 10/20-page profile.
- Mission 13, AVX-VNNI/AVX-512: blocked because this campaign ran on Apple M4 and
  had no matching x86 host; no cross-architecture extrapolation is claimed.
- Alien candidates A1-A5 and B1-B3 remain design proposals. None is recorded as
  an empirical win; each requires its own hypothesis, conservative fallback,
  conformance proof, and measured keep/revert decision before implementation.

## Completed Passes

### Pass 1 - Streaming source BF16 through decode GEMVs

- Result: NEGATIVE, reverted.
- Correctness: unit-level `to_bits` equality and six byte-identical page outputs.
- Performance: median dense decode 13.94 s -> 14.58 s (+4.59%); lm_head
  4,783 ms -> 5,139 ms (+7.44%).
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-bf16-stream/`.
- Ledger: `CLAIM-bd-2mo30-bf16-streaming-decode` in
  `docs/NEGATIVE_EVIDENCE.md`.
- Retry predicate: native packed-BF16 consumption that amortizes conversion and
  preserves the accepted decode accuracy contract.

### Pass 2 - Parallel independent heads in bit-exact R-SWA

- Result: POSITIVE, retained behind `FOCR_RSWA_PARALLEL_ATTN=0` fallback.
- Normal-page A/B: all three adjacent comparisons favored parallel; median
  decode 15.11 s -> 14.19 s (-6.09%) and attention 6,080 ms -> 5,254 ms
  (-13.59%).
- Correctness: six normal-page outputs and both long-sentinel outputs were
  byte-identical; the 1/2/8/10-thread private-pool matrix passed.
- Sentinel performance: page `0590` total 928.81 s -> 884.20 s (-4.80%) and
  attention 364,725 ms -> 317,677 ms (-12.90%).
- Independent blocker: both sentinel modes reproduced the conservative-recipe
  32,768-token max-length runaway (`bd-2mo.30.12`), separating that release
  failure from the bit-exact scheduling win.
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-rswa-par-heads/`.
- Ledger: `CLAIM-bd-2mo30-rswa-parallel-heads` in `docs/PERF_LEDGER.md`.

### Pass 3 - Interleave f32 lm-head output rows

- Result: NEGATIVE, reverted.
- Hypothesis: reuse each normalized hidden activation load across 2 or 4
  independent vocabulary rows while retaining each row's eight-lane
  accumulation and reduction order exactly.
- Scope: high-precision `lm_head` only; no shared projection kernel change.
- Correctness: the focused bit-identity matrix passed, and all six page outputs
  were byte-identical.
- Performance: baseline rows=1 median decode/lm-head 12.715 s/4,905.5 ms;
  rows=2 14.34 s/6,329 ms (+12.78%/+29.02%); rows=4
  14.60 s/6,829 ms (+14.82%/+39.21%).
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-lmhead-row-interleave/`.
- Retry predicate: a native multi-row microkernel with inspected vector code and
  measured activation-load reuse; do not retry the scalar nested-row loop.

### Pass 4 - Existing f32 lm-head contiguous vocab tiling

- Result: NEGATIVE, existing switch remains default-off.
- Hypothesis: smaller contiguous vocabulary panels may improve cache residency
  without changing any per-logit reduction.
- Scope: existing `FOCR_LMHEAD_SHARD`/`FOCR_LMHEAD_SHARD_TILES`; no source lever.
- Fallback: environment unset selects the monolithic head.
- Correctness: all five page outputs were byte-identical.
- Performance: bracketed monolithic median decode/lm-head 12.965 s/5,035.5 ms;
  tiles=2 13.13 s/5,163 ms (+1.27%/+2.53%); tiles=8
  13.68 s/5,680 ms (+5.52%/+12.80%); tiles=32
  14.62 s/6,623 ms (+12.77%/+31.53%).
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-lmhead-vocab-tiles/`.

### Pass 5 - Whole-forward worker-count sweep

- Result: NEGATIVE for changing the current eight-worker benchmark setting.
- Hypothesis: the ten independent attention heads and bandwidth-heavy f32 head
  may favor a worker count other than the inherited eight-thread setting.
- Scope: `FOCR_THREADS`, `RAYON_NUM_THREADS`, and `OMP_NUM_THREADS` move together;
  no math or source changes.
- Fallback: retain eight workers unless a stable adjacent comparison wins.
- Correctness: all six page outputs were byte-identical.
- Performance: median decode at 8/6/10 workers was 13.00/14.05/13.32 s;
  six workers lost 8.08%, while ten lost 2.46%. Ten improved `lm_head` 2.46%
  but regressed attention 6.38% and experts 5.31%.
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-thread-count/`.

### Pass 6 - Mmap-capable `.focrq` loading

- Result: POSITIVE performance result, but REJECTED as the shipping default after
  fresh-eyes safety review. Owned bytes are default; `FOCR_MMAP=1` explicitly
  opts trusted immutable inodes into the measured mmap capability.
- Hypothesis: the newly wired `Weights::load` mmap path should reduce artifact
  ingestion and end-to-end time versus `FOCR_NO_MMAP=1` without changing bytes.
- Scope: loader/storage only; cap decode length for a startup-dominated A/B and
  leave all math switches fixed.
- Safety fallback: environment unset uses buffered owned loading.
- Correctness: all four capped-decode outputs were byte-identical.
- Performance: median wall 7.005 -> 6.63 s (-5.35%); median system CPU
  3.255 -> 2.635 s (-19.05%). Vision/prefill/decode timings stayed stable,
  attributing the gain to load/ownership overhead.
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-mmap-load/`.

### Pass 7 - Chunked prefill schedule

- Result: NEGATIVE, existing switch remains default-off.
- Hypothesis: splitting the 277-token reference prefill may improve cache
  locality enough to repay repeated layer/scheduling overhead.
- Scope: existing bit-exact `FOCR_PREFILL_CHUNK` values 256/128/64; capped decode
  keeps the experiment focused and short.
- Fallback: environment unset selects the monolithic prefill.
- Correctness: all five capped-decode outputs were byte-identical.
- Performance: bracketed monolithic median prefill 0.625 s; chunks 256/128/64
  took 0.83/0.98/1.24 s (+32.8%/+56.8%/+98.4%).
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-prefill-chunk/`.

### Pass 8 - Transfer softmax output ownership

- Result: POSITIVE, retained.
- Hypothesis: `ft_kernel_cpu` already allocates the final softmax result, so
  assigning it into `Mat::data` avoids a redundant full-payload copy. SAM makes
  this material by applying row softmax to every attention head.
- Scope: ownership only in `nn::softmax_rows`; arithmetic and output layout are
  unchanged.
- Historical fallback: source revert if page hashes or focused tests differ, or
  if the measured SAM/vision effect is neutral. Current-tree revalidation uses
  truthy `FOCR_SOFTMAX_COPY=1` to restore that copy in the same binary; unset or
  falsy values retain ownership transfer.
- Correctness: focused softmax tests passed; all six page outputs were
  byte-identical across separately hashed before/after binaries.
- Performance: median SAM blocks 3.36 -> 3.25 s (-3.27%), SAM forward
  3.44 -> 3.34 s (-2.91%), vision tower 4.56 -> 4.44 s (-2.63%), and user CPU
  32.20 -> 31.39 s (-2.52%).
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-softmax-ownership/`.

### Pass 9 - Cache Unlimited SAM/projector statics

- Result: POSITIVE, retained with falsy environment fallback.
- Hypothesis: load-once batches should hydrate SAM and transpose the 2048x1280
  projector once per model, not once per page/view.
- Scope: one model-owned immutable cache shared by sequential and batched vision;
  CLIP already follows this pattern.
- Fallback: source revert if multi-page bytes, deadlock gates, retained-memory
  bounds, or throughput attribution fail.
- Correctness: cached/pretransposed projector unit gate passed; timing-stripped
  five-page JSON results were byte-identical with cache off/on.
- Performance: same-binary cache-on reduced real 29.29 -> 28.19 s (-3.76%),
  later-page median 5.619 -> 5.402 s (-3.86%), and later-page vision
  4.095 -> 3.930 s (-4.03%). Maximum RSS fell by 106.2 MB (-0.91%).
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-unlimited-vision-cache/`.

### Pass 10 - Effective Apple int8 route

- Result: POSITIVE, retained with `FOCR_INT8_AUTOVEC=0` fallback.
- Hypothesis: the default LLVM-autovectorized dense int8 contraction remains
  faster than the nominal SDOT route on the current conservative decoder.
- Scope: `FOCR_INT8_AUTOVEC=0` versus default, with all non-int8 stages fixed and
  a capped decode; output bytes must match.
- Fallback: `FOCR_INT8_AUTOVEC=0` restores the hand-written SDOT dispatch.
- Correctness: all four valid outputs were byte-identical; one operator-error
  invocation is preserved in the manifest but excluded from measurement.
- Performance: forced SDOT median decode/expert time 0.915 s/269.5 ms -> LLVM
  autovec 0.830 s/180.0 ms (-9.29%/-33.21%).
- Observability: `robot backends` now separates hardware ISA selection from the
  effective ordinary-dense route; selftest records branch-derived executed
  routes. Forced x86 selection was also corrected to reach the named branch.
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-apple-int8-autovec/`.
- Ledger: `CLAIM-bd-2mo30-apple-int8-autovec`.

### Pass 11 - Forced Apple dense-int8 tier sweep

- Result: NEGATIVE for SDOT/SMMLA promotion; routes retained for portability and
  parity proof.
- Hypothesis: a forced intrinsic tier might outperform scalar/autovec after the
  conservative recipe narrowed int8 work to FFN/expert matrices.
- Scope: fresh processes forced to `sdot`, `smmla`, or `scalar`; two samples per
  tier in reverse order; capped decode and all non-int8 stages fixed.
- Correctness: all six outputs were byte-identical, sha256
  `88c668f6a8bed89c014f52ccac478abcccde0e296e483b27c385dafc9570bab4`.
- Performance: scalar/autovec-equivalent median decode/expert time
  0.830 s/180.5 ms; SDOT +10.84%/+48.75%; SMMLA +71.08%/+327.42%.
- Disposition: keep the forced branches and all-tier subprocess proof; retain
  autovec as the Apple ordinary-dense default.
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/ab-arm-tier-force/`.
- Ledger: `CLAIM-bd-2mo30-arm-tier-force`.

### Pass 12 - PGO-only build spike

- Result: NEGATIVE, plain `release-perf` build retained.
- Hypothesis: profile-guided layout/inlining over the representative decode
  corpus can reduce branch-heavy decoder overhead without changing arithmetic.
- Scope: plain `release-perf` versus PGO-use build, identical source and runtime
  flags; no BOLT claim on this Mach-O host.
- Correctness: page output was byte-identical; timing-stripped capped ten-page
  JSON was semantically identical.
- Performance: PGO reduced page decode by 14.52% and page wall by 8.00%, but
  regressed prefill by 57.63%. On the capped ten-page workload it reduced decode
  by 14.52% while regressing prefill by 56.12%, leaving only a 1.14% wall win.
- Disposition: the precommitted gate required at least 2% wins on both decode
  and capped ten-page wall. The batch gate failed, so no PGO flags are retained.
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/pass12-pgo-20260710T073736Z/`.
- Ledger: `CLAIM-bd-2mo30-pgo-whole-program`.
- Retry predicate: use a final-tree, phase-selective or prefill-weighted profile
  that removes the measured prefill regression while preserving the decode win,
  then rerun the same page and capped ten-page gate.

### Pass 13 - page_0590 precision attribution

- Result: NEGATIVE for full-int8; conservative default retained and release
  blocker `bd-2mo.30.12` remains open.
- Hypothesis: the accepted conservative FFN/expert-only int8 recipe preserves
  materially more of the pinned BF16 reference than the experimental full-int8
  attention/lm-head mode on the known page_0590 release blocker.
- Scope: same plain binary, model artifact, 12,000-token cap, thread settings,
  and page; compare conservative gates-unset versus all three experimental
  int8 gates enabled. This is diagnostic evidence only, never a promotion of
  full-int8 under unresolved OQ-14.
- Correctness: conservative normalized CER was `1.24286`; full-int8 regressed
  it to `1.63831` (+0.39545 absolute, +31.82% relative). Both modes exhausted
  all 12,000 generated tokens without EOS and therefore fail the release gate.
- Performance: full-int8 reduced decode 318.23 -> 144.63 s (2.20x), but speed
  cannot override the measured accuracy loss.
- Disposition: keep all experimental attention/lm-head int8 gates off. Add a
  deterministic repetition/EOS fallback and rerun the pinned BF16 comparison;
  do not hide the blocker by promoting full-int8.
- Evidence: `artifacts/perf/bd-2mo.30/profile-recipe-5733407/pass13-page0590-precision-20260710T082044Z/`.
- Ledger: `CLAIM-bd-2mo30-page0590-full-int8-rejection`.

### Pass 14 - Final conservative reprofile and audit

- Status: COMPLETED AUDIT at 2026-07-10T11:23:40Z; not release-certified.
- Scope: integrate the gauntlet, distribution, loader-shape, and one-live-forward
  hardening; run focused selftests, final conservative profile/route checks,
  full Rust gates, ledgers, release-readiness certification, and bead triage.
- Stop rule: no new optimization lever. This pass verifies the final tree,
  records remaining red gates honestly, and closes only work proven green.
- Rust verification: `cargo fmt --check`, RCH-wrapped `cargo check
  --all-targets`, `cargo clippy --all-targets -- -D warnings`, and the hard
  `cargo test` gate passed. The library reported 1,006 passed and one ignored;
  every integration, golden, watchdog, property, parity, and doc-test lane was
  green. RCH reported path-normalization failure and fell open, so
  `bd-2mo.30.14` owns actual execution-route and remote-toolchain attestation.
- Runtime verification: `robot backends` reports effective `autovec` separately
  from `sdot` hardware capability; `robot selftest` passed 44/44 routes.
- Evidence verification: all 16 campaign checksum manifests and all 287 entries
  verified. CER comparison covered 16,129 exhaustive and 10,000 random pairs;
  reference/roofline/timing selftests passed 97/42/54 checks; both shell harnesses,
  shellcheck, and all 988 ledger checks passed.
- Receipt verification: row selftest passed 123/123, certificate selftest passed
  194/194, reference producer selftest passed 101/101, and the Unlimited entry
  selftest passed 18/18. The ten-input physical replay contract covers timing,
  correctness, binary, build/source, workspace/local-dependency/Cargo-config
  bytes, and pinned reference provenance mutations. The measured 250.44 MiB
  source pack streams through manifesting, replay snapshotting, and final
  verification without the former 64 MiB buffering limit.
- UBS: the Python diff scan exited zero with no critical findings. The Rust diff
  scan exited one because UBS v5.0.3 classifies 21 `panic!` calls inside
  `#[cfg(test)]` modules as critical; all 21 were manually localized to test
  assertions, no production `panic!` was added, and fmt/clippy/test remained
  green. The warnings were not hidden by changing test semantics or ignore rules.
- Pass-14 fail-closed verdict: convergence had 11 rounds and a clean 0/0 tail,
  while `EXP-1403` page-0590 calibration was still open. Release readiness was
  4 green / 10 red and rejected the legacy full-int8/stale performance bundles,
  missing audit/dist/signature receipts, partial MUST surfaces, and unresolved
  calibration. Pass 15 supersedes only that local page/corpus blocker; it does
  not retroactively make the Pass-14 certificate or distribution bundle green.

### Pass 15 - Exact Torch routing hard-page and corpus acceptance

- Status: ACCEPTED LOCAL CORRECTNESS EVIDENCE at 2026-07-10T18:26:36Z;
  unpublished and not release-certified.
- Scope: current release binary with exact Torch 2.10 CPU
  `topk(sorted=False)` slot ordering, conservative FFN/expert-only int8 model,
  all experimental numerical/runtime gates unset, 12,000-token per-page cap,
  and the pinned transformed BF16 comparison.
- Hard page: `page_0590` emitted EOS after 4,033 generated tokens. Normalized
  CER is `0.6164885983`; termination is accepted, but this high tail remains a
  named limitation and is not represented as exact parity.
- Corpus: all 20 sorted pages were present and successful. Aggregate normalized
  CER is `0.1930792459`, within the predeclared `0.25` budget.
- Identity: binary sha256
  `6a401be3bfd6bfb0a9766e2ff333489a5e02b2a5f0641376b6ab5fc6885e6953`;
  model sha256
  `573340710167697891bf52dfa4cbb5d0a02a68f3011c01f8ef83fd34622fb592`;
  page output sha256
  `57d9b5a6686e4c8f877b42390c74b85f77ef789036351cfbd0f373194d10997a`;
  batch sha256
  `7d6e1f5d7823c4a297b5fcc07f7ade44a0ce36f0cd89a14f4d6a424345db572c`;
  corpus comparison sha256
  `38680e64677ab30f4842aa53fa98eb9c1ef0caefca3e293163f1e34b501d9f43`.
- Controller disposition: close `EXP-1403` as `NO_EVIDENCE`. The opt-in
  runaway controller was not promoted or needed after exact routing removed
  the no-EOS failure.
- Evidence:
  `artifacts/perf/bd-2mo.30/profile-recipe-5733407/pass15-exact-torch-corpus-20260710T182636Z/`.
- Remaining blockers: publish a compatible exact-recipe artifact, regenerate
  the clean-tree gauntlet/readiness bundle, complete distribution/signing and
  platform receipts, and close the remaining tracker gates. No ship, tag,
  release, performance-certificate, or publication claim is made by this pass.

## Stop Conditions

- Two consecutive zero-change passes with three or more named checks each.
- Thrashing over the same lines or mutually inconsistent constraints.
- Pass cap reached.
- The user-specified performance/conformance target is proven by current artifacts.

No pass may claim completion while its correctness, attribution, focused/broad benchmark, noise, fallback, ledger, or quality gate is missing.
