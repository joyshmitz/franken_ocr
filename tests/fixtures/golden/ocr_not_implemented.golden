focr: model not found / not resolvable: no model artifact named models/unlimited-ocr.focrq (searched directories: [model-search-dirs]; set FOCR_MODEL_DIR or pass an explicit path) — run `focr pull` to install the compatible conservative default, point FOCR_MODEL_PATH at raw BF16 weights or an exact-recipe local artifact, or inspect `focr models` for other compatible pulls
